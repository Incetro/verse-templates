{% include 'header' %}

import Foundation

// MARK: - {{ module_info.name }}State

/// `{{ module_info.name }}` module state
///
/// Basically, `{{ module_info.name }}State` is a type that describes the data
/// `{{ module_info.name }}` feature needs to perform its logic and render its UI.
public struct {{ module_info.name }}State: Equatable {

    // MARK: - Properties
}
